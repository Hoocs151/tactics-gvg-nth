import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No special config needed for Tailwind CSS v4 with Next.js 16
  // The globals.css with @import "tailwindcss" handles everything
};

export default nextConfig;
